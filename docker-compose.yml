services:
  transport-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: transport-api
    restart: unless-stopped
    environment:
      APP_ENV: development
      APP_PORT: "8080"
      DB_HOST: aws-1-ap-northeast-2.pooler.supabase.com
      DB_PORT: "6543"
      DB_USER: postgres.eskuttiwukrwipmzwkgt
      DB_PASSWORD: "711771Wira@rbaa"
      DB_NAME: postgres
      DB_SSLMODE: require
      DB_MAX_CONNS: "20"
      JWT_ACCESS_SECRET: dev_access_secret
      JWT_REFRESH_SECRET: dev_refresh_secret
      JWT_ACCESS_TTL: 15m
      JWT_REFRESH_TTL: 168h
    ports:
      - "8080:8080"
    networks:
      - transport-net

networks:
  transport-net:
    name: transport-net
    driver: bridge
